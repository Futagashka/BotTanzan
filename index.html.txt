<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kipashio ⚡ Aviator (Kimeharibika!)</title>
  <style>
    @keyframes pulse {
      0% { box-shadow: 0 0 5px #0f0; }
      50% { box-shadow: 0 0 20px #0f0; }
      100% { box-shadow: 0 0 5px #0f0; }
    }
    body{
      font-family: 'Trebuchet MS', sans-serif;
      background:#000;
      color:#0f0;
      text-align:center;
      padding:40px;
      line-height:1.5;
    }
    h1{
      font-size:2.2rem;
      margin-bottom:10px;
    }
    #subtitle{
      font-size:1.1rem;
      margin-bottom:30px;
    }
    #signals{
      margin-bottom:25px;
      font-size:1.6rem;
      min-height:60px;
    }
    button{
      background:#0f0;
      color:#000;
      border:none;
      padding:12px 28px;
      font-size:1.3rem;
      cursor:pointer;
      border-radius:8px;
      animation:pulse 2s infinite;
    }
    button:hover{
      background:#1aff1a;
    }
    #modal{
      position:fixed;
      inset:0;
      background:rgba(0,0,0,0.85);
      display:flex;
      align-items:center;
      justify-content:center;
      visibility:hidden;
      z-index:1000;
    }
    #modalContent{
      background:#111;
      padding:35px 25px;
      border:2px solid #0f0;
      border-radius:16px;
      max-width:90%;
      width:420px;
    }
    #modal h2{
      margin-top:0;
      font-size:1.8rem;
    }
    #modal a{
      display:inline-block;
      margin-top:25px;
      background:#0f0;
      color:#000;
      padding:12px 30px;
      text-decoration:none;
      font-weight:bold;
      border-radius:10px;
    }
    #modal a:hover{
      background:#1aff1a;
    }
  </style>
</head>
<body>
  <h1>🎲 Kipashio cha Aviator ⚡</h1>
  <p id="subtitle">💸 Pata ishara <strong>2</strong> za bure kisha 🤑 unlock mapato kamili kwa kujisajili!</p>

  <div id="signals"></div>
  <button id="getSignalBtn">🎯 Pata Ishara</button>

  <div id="modal">
    <div id="modalContent">
      <h2>⛔ Ufikiaji Umekatizwa</h2>
      <p>Ili kuona ishara zote na <strong>kuchota faida 💰</strong>, bofya kitufe hapa chini na <strong>jisajili sasa</strong>:</p>
      <a href="https://affpa.top/L?tag=d_4340895m_15639c_&site=4340895&ad=15639&r=registration" target="_blank">🚀 Jisajili</a>
    </div>
  </div>

  <script>
    const signalsDiv = document.getElementById('signals');
    const btn = document.getElementById('getSignalBtn');
    const modal = document.getElementById('modal');
    let signalCount = 0;

    function getRandomSignal() {
      const coefficient = (Math.random() * 1.5 + 1).toFixed(2);
      return `📈 Ishara #${signalCount}: Kuzidisha kutakuwa ${coefficient}x`;
    }

    btn.addEventListener('click', () => {
      signalCount++;
      if (signalCount <= 2) {
        const signalText = getRandomSignal();
        const p = document.createElement('p');
        p.textContent = signalText;
        signalsDiv.appendChild(p);
      }
      if (signalCount === 2) {
        setTimeout(() => {
          modal.style.visibility = 'visible';
        }, 1000);
      }
    });

    modal.addEventListener('click', (e) => {
      if (e.target === modal) {
        modal.style.visibility = 'hidden';
      }
    });
  </script>
</body>
</html>
